# Themes Directory

This directory is reserved for user-created theme exports.

In Scopi v2, themes are **dynamically generated** during `/scopi:setup` based on your brand interview. The generated theme lives inline inside `scopi.config.json` — not as a separate file.

If you want to share or backup your theme, export it here as a JSON file for reference.
